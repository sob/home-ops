---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-minio-0
  namespace: default
spec:
  storageClassName: nfs-provision-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
