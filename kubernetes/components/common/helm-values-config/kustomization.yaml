---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
metadata:
  name: helm-values-config
# This component provides nameReference configuration for HelmReleases
# that use valuesFrom with ConfigMaps generated by Kustomize
configurations:
  - kustomize-config.yaml