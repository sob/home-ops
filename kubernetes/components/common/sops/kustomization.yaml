---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # - ./secret.sops.yaml # sops-age secret is created during bootstrap
  # - ./external-secret.yaml # TODO: Uncomment after external-secrets is installed
