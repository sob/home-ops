---
name: Build App

on:
  workflow_call:
    inputs:
      app:
        description: "Application to build"
        required: true
        type: string
      release:
        description: "Release build"
        required: true
        type: boolean

env:
  REGISTRY: ghcr.io

jobs:
  plan:
    runs-on: ubuntu-latest
    outputs:
      platforms: ${{ steps.platforms.outputs.platforms }}
      version: ${{ steps.version.outputs.version }}
      major_version: ${{ steps.version.outputs.major_version }}
      raw: ${{ steps.version.outputs.raw }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check if app exists
        run: |
          if [ ! -d "containers/${{ inputs.app }}" ]; then
            echo "App ${{ inputs.app }} does not exist"
            exit 1
          fi
          if [ ! -f "containers/${{ inputs.app }}/docker-bake.hcl" ]; then
            echo "App ${{ inputs.app }} does not have docker-bake.hcl"
            exit 1
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Get platforms
        id: platforms
        working-directory: containers/${{ inputs.app }}
        run: |
          platforms=$(docker buildx bake --print | jq -r '.target[].platforms // [] | @json')
          echo "platforms=$platforms" >> $GITHUB_OUTPUT

      - name: Get version
        id: version
        working-directory: containers/${{ inputs.app }}
        run: |
          echo "=== Docker bake output ==="
          docker buildx bake --print
          echo "=== Extracting versions ==="
          # Try different jq paths to find the variables
          bake_output=$(docker buildx bake --print)
          version=$(echo "$bake_output" | jq -r '.variable.VERSION.default // empty')
          if [ -z "$version" ]; then
            version=$(echo "$bake_output" | jq -r '.variables.VERSION.default // empty')
          fi
          if [ -z "$version" ]; then
            version=$(echo "$bake_output" | jq -r '.target.mysticbbs.args.VERSION // empty')
          fi
          if [ -z "$version" ]; then
            version="rolling"
          fi
          
          major_version=$(echo "$bake_output" | jq -r '.variable.MAJOR_VERSION.default // empty')
          if [ -z "$major_version" ]; then
            major_version=$(echo "$bake_output" | jq -r '.variables.MAJOR_VERSION.default // empty')
          fi
          if [ -z "$major_version" ]; then
            major_version=$(echo "$bake_output" | jq -r '.target.mysticbbs.args.MAJOR_VERSION // empty')
          fi
          echo "Extracted version: '$version'"
          echo "Extracted major_version: '$major_version'"
          echo "version=$version" >> $GITHUB_OUTPUT
          echo "major_version=$major_version" >> $GITHUB_OUTPUT
          echo "raw=$version" >> $GITHUB_OUTPUT

  build:
    runs-on: ${{ matrix.runner }}
    needs: plan
    if: ${{ fromJson(needs.plan.outputs.platforms) != null }}
    strategy:
      fail-fast: false
      matrix:
        platform: ${{ fromJson(needs.plan.outputs.platforms) }}
        include:
          - platform: linux/amd64
            runner: ubuntu-24.04
          - platform: linux/arm64
            runner: ubuntu-24.04-arm
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build App Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ inputs.app }}
          tags: |
            type=raw,value=
          labels: |
            org.opencontainers.image.title=${{ inputs.app }}
            org.opencontainers.image.url=https://github.com/${{ github.repository_owner }}/home-ops
            org.opencontainers.image.version=${{ needs.plan.outputs.version }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.vendor=${{ github.repository_owner }}
          bake-target: ${{ inputs.app }}

      - name: Build App
        id: build
        uses: docker/bake-action@v5
        with:
          workdir: containers/${{ inputs.app }}
          targets: ${{ inputs.app }}
          set: |
            *.platform=${{ matrix.platform }}
            *.output=type=image,name=${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ inputs.app }},push-by-digest=true,name-canonical=true,push=true
            *.cache-from=type=gha
            *.cache-to=type=gha,mode=max
          files: |
            ./docker-bake.hcl
            ${{ steps.meta.outputs.bake-file }}

      - name: Export digest
        run: |
          mkdir -p /tmp/digests
          digest="${{ fromJSON(steps.build.outputs.metadata)[inputs.app]['containerimage.digest'] }}"
          touch "/tmp/digests/${digest#sha256:}"

      - name: Upload digest
        uses: actions/upload-artifact@v4
        with:
          name: digests-${{ inputs.app }}-${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  merge:
    runs-on: ubuntu-latest
    needs: [plan, build]
    steps:
      - name: Download digests
        uses: actions/download-artifact@v4
        with:
          pattern: digests-${{ inputs.app }}-*
          path: /tmp/digests
          merge-multiple: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create manifest list and push
        working-directory: /tmp/digests
        run: |
          # Create tags from docker-bake.hcl configuration
          IMAGE_NAME="${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ inputs.app }}"
          echo "Available digest files:"
          ls -la
          DIGESTS=""
          for digest in *; do
            echo "Processing digest: $digest"
            DIGESTS="$DIGESTS $IMAGE_NAME@sha256:$digest"
          done
          echo "Final digests: $DIGESTS"
          echo "=== Version Information ==="
          echo "Version: '${{ needs.plan.outputs.version }}'"
          echo "Major Version: '${{ needs.plan.outputs.major_version }}'"
          echo "Raw: '${{ needs.plan.outputs.raw }}'"
          
          # Create rolling tag first
          echo "Creating rolling tag..."
          docker buildx imagetools create -t ${IMAGE_NAME}:rolling $DIGESTS
          
          # Create version tag
          if [ -n "${{ needs.plan.outputs.version }}" ] && [ "${{ needs.plan.outputs.version }}" != "rolling" ]; then
            echo "Creating version tag: ${{ needs.plan.outputs.version }}"
            docker buildx imagetools create -t ${IMAGE_NAME}:${{ needs.plan.outputs.version }} ${IMAGE_NAME}:rolling
          else
            echo "Skipping version tag - empty or rolling"
          fi
          
          # Create major version tag if defined
          if [ -n "${{ needs.plan.outputs.major_version }}" ]; then
            echo "Creating major version tag: ${{ needs.plan.outputs.major_version }}"
            docker buildx imagetools create -t ${IMAGE_NAME}:${{ needs.plan.outputs.major_version }} ${IMAGE_NAME}:rolling
          else
            echo "Skipping major version tag - not defined"
          fi

      - name: Inspect image and extract digest
        id: inspect
        run: |
          IMAGE_NAME="${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ inputs.app }}"
          # Get digest from the digest files created during build
          cd /tmp/digests
          if ls *.* > /dev/null 2>&1; then
            DIGEST=$(ls | head -1)
            echo "Found digest: $DIGEST"
            echo "digest=$DIGEST" >> $GITHUB_OUTPUT
          else
            echo "No digest files found, skipping SHA256 tag creation"
            echo "digest=" >> $GITHUB_OUTPUT
          fi

      - name: Tag with SHA256 digest
        if: ${{ steps.inspect.outputs.digest != '' }}
        run: |
          IMAGE_NAME="${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ inputs.app }}"
          docker buildx imagetools create \
            -t ${IMAGE_NAME}:sha256-${{ steps.inspect.outputs.digest }} \
            ${IMAGE_NAME}:rolling

  test:
    runs-on: ubuntu-latest
    needs: [plan, merge]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Run container tests
        working-directory: containers/${{ inputs.app }}
        run: |
          if [ -f "container_test.go" ]; then
            echo "Running container tests for ${{ inputs.app }}"
            go test -v .
          else
            echo "No container tests found for ${{ inputs.app }}"
          fi